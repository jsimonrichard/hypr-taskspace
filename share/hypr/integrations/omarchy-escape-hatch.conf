# Emergency terminal — bypasses tsk entirely.
# SUPER+Return is task-scoped via tsk; use SUPER+Ctrl+Return when tsk or tskd is broken.
bindd = SUPER CTRL, RETURN, Emergency terminal, exec, xdg-terminal-exec
