// Legacy Waybar exec-module reference (superseded by cffi/lae).
// Current install uses the Rust CFFI module — see README.md.
//
// Old exec modules:
//   custom/lae-task       — current task name
//   custom/lae-workspace-N — workspace N within current taskspace
